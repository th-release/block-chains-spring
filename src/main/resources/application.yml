spring:
  datasource:
    url: jdbc:mysql://localhost:3306/block-chains
    username: root
    password: xogur38997
  driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties: # property ?? ??
      hibernate: # hibernate property ??
        format_sql: true
  servlet:
    multipart:
      max-request-size: 10MB

server:
  servlet:
    contextPath: /api
